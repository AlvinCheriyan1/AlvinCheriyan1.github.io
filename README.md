# Alvin Cheriyan

A minimal personal website: a white page, a short introduction, and two links.

Open `index.html` in a browser. No build, dependencies, or JavaScript required.

Edit the text and links in `index.html`. Layout and typography are in `styles.css`.

Ready to serve directly from the repository root with GitHub Pages.
